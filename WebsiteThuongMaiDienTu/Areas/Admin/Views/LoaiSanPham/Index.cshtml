@using WebsiteThuongMaiDienTu.Models
@model List<loaisanpham>
@{
    ViewBag.Title = "Quản lý Loại sản phẩm";
    bool isAdmin = ViewBag.IsAdmin == true;
}

<div class="kh-container">
    <div class="kh-card">
        <div class="kh-header">
            <h2>
                <i class="bi bi-tags-fill" style="color: var(--brand-yellow);"></i>
                Quản lý Loại sản phẩm
            </h2>

            @if (isAdmin)
            {
                <a href="@Url.Action("TaoMoi", "LoaiSanPham", new { area = "Admin" })" class="btn-them">
                    <i class="bi bi-plus-circle"></i> Thêm loại mới
                </a>
            }
        </div>

        <div class="table-responsive">
            <table class="kh-table">
                <thead>
                    <tr>
                        <th style="width: 150px;">Mã loại</th>
                        <th>Tên loại sản phẩm</th>
                        <th style="width: 120px;" class="text-center">Hành động</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td><strong>@item.maloaisanpham</strong></td>
                            <td>@item.tenloaisanpham</td>
                            <td class="text-center">
                                @if (isAdmin)
                                {
                                    <a href="@Url.Action("Sua", "LoaiSanPham", new { area = "Admin", id = item.maloaisanpham })"
                                       class="btn-icon btn-sua"
                                       title="Sửa">
                                        <i class="bi bi-pencil-fill"></i>
                                    </a>

                                    <a href="@Url.Action("Xoa", "LoaiSanPham", new { area = "Admin", id = item.maloaisanpham })"
                                       class="btn-icon btn-xoa"
                                       title="Xóa"
                                       onclick="return confirm('Bạn có chắc chắn muốn xóa loại sản phẩm này?');">
                                        <i class="bi bi-trash-fill"></i>
                                    </a>
                                }
                                else
                                {
                                    <span class="text-muted">Chỉ xem</span>
                                }
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>